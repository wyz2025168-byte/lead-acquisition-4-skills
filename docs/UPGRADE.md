# 从旧版升级

1. 备份当前项目和旧 Skill；不要直接覆盖唯一的 `.jw-project`。
2. 安装 v3 后运行 `doctor`，确认版本为 `3.0.0`、七方法完整且没有旧 `jw-*` 残留。
3. 对旧项目运行 `migrate`。运行时先建立只读备份，只导入来源、已确认事实和历史审批记录。
4. 旧定位、人群、脚本、拍摄和运营结论不会自动生效，必须按 v3 方法重新资格审查。
5. 新项目优先从空目录开始，以免历史判断影响从零测试。

运行时不会擅自删除用户文件，也不会自动执行发布或联系客户。
