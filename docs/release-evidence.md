# 发行证据与能力边界

> v1 当前状态：`Preview / Structural Validation Only`。本页纠正早期对结构测试结果的过度表述。

## 私有编译验证摘要

| 阶段 | 结果 |
|---|---|
| 真源与课程编译 | 已完成来源校验、课程地图、合同冻结与能力编译 |
| 确定性结构单元测试 | 历史结果 70 / 70；未真实调用 GPT 或安装后的 Skill |
| 接口与主链夹具 | 预制输入下的 Schema、顺序、边界和失效传播结构检查通过 |
| 案例夹具回放 | 使用预写结论与 PASS 标记的结构回放，不构成盲测 |

私有题目、答案、Rubric、来源编号、文件路径、哈希、审批号和逐决策追溯不随公开包分发。

## 尚未包含

G7 真实项目和 G8 连续运行尚未通过。七个课程能力统一重新定性为 `PROVISIONAL_STRUCTURAL_VERIFIED`。本发行包只代表“可安装、文件完整、结构夹具能运行”，不代表课程判断已经被真实模型复现，更不代表业务效果或生产可用性。

## 为什么重新定性

早期 G4 使用关键词分类和硬编码答案；G5 使用预制完美夹具；G6 预先写入案例结论和 PASS。它们可以继续作为结构回归测试，但不能再代表业务能力验收。v2 将以全新 Codex 项目实际安装、真实模型生成的上游成果、跨任务恢复、匿名盲投和连续真实任务重新验收。

## 发行变换

当前修订只更正公开状态和证据解释，不修改 v1 的课程规则、输入输出 Schema、owner 或业务判断。v1 已冻结；能力重建只在私有 v2 工作区进行。
