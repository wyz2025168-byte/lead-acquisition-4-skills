# v4 自然项目评测

场景只描述自然业务需求、后续事实或用户纠正，不规定方法、节点、人物、CTA、脚本数量或预期错误码。生成者只看到场景和候选 `$jw`；评分者只看到匿名输出与冻结 Rubric。

先运行 `python3 evals/natural/run_eval.py --scan`。正式运行把每个场景独立执行三次，原始输出写入不进入公开发行包的外部结果目录。任何硬失败使该场景失败；不得用差异修复代替完整重跑。
