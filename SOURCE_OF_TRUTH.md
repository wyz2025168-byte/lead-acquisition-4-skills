# Source of Truth

v4 候选以本仓库 `skills/jw/` 为唯一运行真源，由 `tools/build-release.py` 单向生成 Manifest 和压缩包。`v3.0.0` tag 保持历史只读；压缩包和本地安装不得反向覆盖候选真源。

公开包只包含编译后的方法程序、B0–O1 内核、业务阶段协议、通用行业适配、合成反例和确定性运行时；不包含原始课程、逐行证据、真实客户资料、历史项目答案或私有评测答案。
